// Pin de un solo uso para la corrida M23 (2026-07-23): el datalake /scratch/datalake SOLO esta montado
// en c002 (c003 no lo tiene y esta casi lleno; gpu1/gpu2 down). Fija el controlador y TODAS las tareas
// (folds incluidas) a c002 sin tocar el repo. Va como ultimo -c -> gana sobre el clusterOptions vacio
// del profile (slurm_account/slurm_qos son null).
process.clusterOptions = '--nodelist=c002'
